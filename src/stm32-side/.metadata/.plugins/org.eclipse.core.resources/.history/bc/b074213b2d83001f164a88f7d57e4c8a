#include "hall.h"

bool hallCheck(GPIO_TypeDef* port, const uint16_t pin){
	GPIO_PinState sensorState = HAL_GPIO_ReadPin(DISK_HALL_GPIO_Port, DISK_HALL_Pin);
	if (sensorState == GPIO_PIN_RESET) {
		return 1;
	}else{
		return 0
	}
}
